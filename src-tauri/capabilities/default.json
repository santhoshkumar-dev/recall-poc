{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capability for the Recall main window; filesystem access is only exposed by validated Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}

