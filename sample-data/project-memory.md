# Recall validation notes

The private search prototype must continue working after Wi-Fi is disconnected.
All OCR, text embeddings, keyword retrieval, and semantic ranking happen locally.

## Known evaluation query

Search for **which prototype works without internet**. The expected result is this note,
with a source snippet and the original file path.
